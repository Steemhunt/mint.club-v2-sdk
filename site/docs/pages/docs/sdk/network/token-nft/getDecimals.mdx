---
description: Returns the decimals of the token.
---

# getDecimals 

Returns the decimals of the token. For mint.club Bonding Curved tokens, it's always `18`

## Usage

```ts twoslash [example.ts]
import { mintclub } from 'mint.club-v2-sdk'
 
const decimals = await mintclub.network('ethereum').token('SYMBOL').getDecimals()
// 18
```

## Return Value

`Promise<number>`

Returns the decimals of the token.